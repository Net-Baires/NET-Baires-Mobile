# Android
# Build your Android project with Gradle.
# Add steps that test, sign, and distribute the APK, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/android

trigger:
- master

pool:
  vmImage: 'macos-latest'

steps:
- task: Npm@1
  inputs:
    command: 'install'
    
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: 'cd Android; ./gradlew  -MYAPP_UPLOAD_STORE_FILE=algoaaaa -MYAPP_UPLOAD_STORE_PASSWORD=aaaaa -MYAPP_UPLOAD_KEY_ALIAS=asdasdasd -MYAPP_UPLOAD_KEY_PASSWORD=123123 assembleRelease'
    
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.SourcesDirectory)/android/app/build/outputs/apk'
    ArtifactName: 'drop'
    publishLocation: 'Container'